# Production Remediation Manifest (v17 Final Closure)

| P0 Item | Defect | Exact Source File / Call Path | Minimal Production Change | Proof Test |
| :--- | :--- | :--- | :--- | :--- |
| **P0-1** | Worker auto-entry bypassed `runCanonicalAutoEntry()` using direct `executeRealOpenOnExchange(symbol, isSellSignal ? 'SHORT' : 'LONG', ...)` | `server.ts` (Worker auto-entry execution branch) | Route worker auto-entry strictly through `runCanonicalAutoEntry()`, binding side exclusively to `getApprovedExecutionSide(entryDecision)`. Fail-closed on veto/mismatch/NO_TRADE before any side-effect. | `scripts/test-production-path-offline.ts`, `server/services/autoEntryService.test.ts` |
| **P0-2** | Paper margin reservation hooks (`reservePaperMargin`, `releasePaperMargin`, `getAvailableVirtualBalance`) declared in `AutoEntryExecutionPort` were not wired into real ports in `server.ts` | `server.ts` (Main & worker paper execution ports) | Implement atomic margin check/debit in `reservePaperMargin`, release on rollback/close in `releasePaperMargin`, and dynamic querying in `getAvailableVirtualBalance` on top of `dbAtomicStore`. | `scripts/test-production-path-offline.ts`, `server/services/autoEntryService.test.ts` |
| **P0-3** | `saveTradeDB()` swallowed write errors via `catch` block; unawaited critical persistence calls in watchdog and event handlers | `server/atomicDbSaver.ts`, `server.ts` | Make `saveTradeDB` and `dbAtomicStore.saveState` reject/throw on persistence failure. Audit and await every critical transition (`PENDING_OPEN`, `OPEN`, `CANCELLED`, `OPEN_UNKNOWN`, partial/full close, balance reservation). | `server/atomicDbSaver.test.ts`, `scripts/test-production-path-offline.ts` |
| **P0-4** | Watchdog had independent trailing profit calculation (`trailingStopActive`) directly triggering `shouldClose = true` outside `evaluateExitPolicy()` | `server.ts` (Watchdog position monitoring loop) | Transfer all trailing/pullback calculations into `evaluateExitPolicy()`. Watchdog only consumes typed `ExitDecision` (`HOLD`, `MOVE_STOP`, `PARTIAL_CLOSE`, `FULL_CLOSE`, `EMERGENCY_FULL_CLOSE`) with strict 4.0% profit floor. | `server/services/exitPolicy.test.ts`, `scripts/test-production-path-offline.ts` |
| **P0-5** | Correlation context was not atomically persisted with Hunter/Bear/Committee envelopes and structured audit records | `server/services/tradeSchema.ts`, `server/services/autoEntryService.ts` | Materialize immutable `DecisionCorrelationContext` for every entry attempt. Durably persist correlated decision bundle. Maintain `LEGACY_UNLINKED` for historical data without fabricating IDs. | `scripts/migrate-trade-audit-fields.test.ts`, `server/services/comprehensiveAuditMatrix.test.ts` |
| **P0-6** | Missing true production-path offline test script executing shared main/worker coordinator code | `package.json`, `scripts/test-production-path-offline.ts` | Implement `"test:production-path:offline"` executing the shared coordinator against isolated temporary DB with injected mock ports covering all P0 test cases. | `npm run test:production-path:offline` |
| **P0-7** | AST verifier in `scripts/verify-integration-wiring.ts` did not check for direct worker real open, missing paper reservation hooks, unawaited saves, or test registration | `scripts/verify-integration-wiring.ts` | Enhance AST validator to inspect call trees, control flows, port hook implementations, and absence of raw `executeRealOpenOnExchange` calls. | `scripts/verify-integration-wiring.ts`, `package.json` |
